/* eslint-disable */

/**
 * Example edge termination values or node types for LangGraph.
 * Only needed if you want a shared enum for graphs.
 */
export enum GraphNodeType {
  Start = 'start',
  End   = 'end',
}
